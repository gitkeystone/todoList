# Todo List 环境变量样例
# 用法：复制为 .env 或直接 export 到 shell（Makefile 透传给后端进程）

# 后端监听端口
PORT=8080

# SQLite 数据库文件路径（相对仓库根目录）
DB_PATH=data/todolist.db

# Gin 运行模式：debug / release
GIN_MODE=debug

# CORS 白名单（逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173

# 生产模式下前端静态资源目录
WEB_DIST=web/dist
